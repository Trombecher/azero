import Config from "tailwindcss";

const config: Config = {
    content: [
        "./index.html",
        "./src/**/*.{js,ts}"
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};

export default config;